## IT Security Agent — scenario_1 — 2026-04-16
**Status:** FAIL
**Required fields present:** NO — `policy_citations[*].section` missing (agent emitted `section_id`, `chunk_id`, `citation_class` per the IT Security Agent Spec and ORCH-PLAN STEP-02 output contract). All other required fields present (`data_classification`, `fast_track_eligible`, `policy_citations[]`, `status`).
**Status signal:** complete / complete — matches scenario-expected.
**Semantic validity:** PASS — `data_classification: UNREGULATED` is consistent with the export-only questionnaire inputs; `fast_track_eligible: true` is consistent with UNREGULATED; both `policy_citations` reference `ISP-001` only (no out-of-lane source citations).
**Notes:** Failure is a governance-doc drift, not a model or prompt failure. `llm_agent_output_evaluation_checklist.md:31` and `per_agent_test_env/evaluators.py:256` require key `section`; `agent_spec_docs/IT_Security_Agent_Spec.md:258`, ORCH-PLAN STEP-02 output contract, and CC-001 §7 provenance require `section_id` (plus `chunk_id`, `citation_class`). Raw recorded at `tests/recorded_responses/it_security_agent__scenario_1__1_fail.json`. Halted per procedure — no re-run.

## IT Security Agent — scenario_1 — 2026-04-16
**Status:** PASS
**Required fields present:** YES — `data_classification`, `fast_track_eligible`, `policy_citations[]`, `status` all present; each `policy_citations` entry carries `source_id`, `version`, `section_id`, `citation_class` (hard), plus `chunk_id` (soft).
**Status signal:** complete / complete — matches scenario-expected.
**Semantic validity:** PASS — `data_classification: UNREGULATED` consistent with EXPORT_ONLY questionnaire inputs; `fast_track_eligible: true` consistent with UNREGULATED; both `policy_citations` reference `ISP-001` only (no out-of-lane sources); `chunk_id` values (`ISP-001__section_12`, `ISP-001__section_17`) match the bundle verbatim; `citation_class: PRIMARY` valid for a Tier 1 source.
**Notes:** Re-run after the evaluator + checklist realignment in session #38 (required keys on `policy_citations` → `section_id` + `citation_class`, `chunk_id` soft). Fixture, spec, and model unchanged. Two minor model drifts vs. run #1 worth flagging but not hard-failed: `eu_personal_data_present` came back as the string `"NO"` instead of a boolean `false` (run #1 returned the boolean), and `fast_track_rationale` came back as the token `"ELIGIBLE_LOW_RISK"` instead of a free-text sentence — both are outside the current hard-check set. Raw recorded at `tests/recorded_responses/it_security_agent__scenario_1__2_pass.json`.

## IT Security Agent — scenario_1 — 2026-04-16
**Status:** PASS
**Required fields present:** YES — all STEP-02 required fields present; each `policy_citations` entry carries the full hard set (`source_id`, `version`, `section_id`, `citation_class`) plus soft `chunk_id`.
**Status signal:** complete / complete — matches scenario-expected.
**Semantic validity:** PASS — `data_classification: UNREGULATED`, `fast_track_eligible: true`, `status: complete` all consistent; citations ISP-001 only; `chunk_id` values match the bundle verbatim. **Spec-change confirmation:** model now emits `eu_personal_data_present: false` (boolean) per the §8.3/§8.4/§9 update in session #40 — run #2's `"NO"` string drift is resolved without any evaluator change.
**Notes:** Re-run after spec-only `eu_personal_data_present` boolean conversion in session #40. Fixture, model, and evaluator unchanged between run #2 and run #3 — only the system prompt (spec doc) moved. `fast_track_rationale` still came back as the token `"ELIGIBLE_LOW_RISK"` rather than free-text — unchanged from run #2, still outside the hard-check set, still worth a future decision. Raw recorded at `tests/recorded_responses/it_security_agent__scenario_1__3_pass.json`.

## Legal Agent — scenario_2 — 2026-04-16
**Status:** FAIL
**Required fields present:** YES — `dpa_required`, `dpa_blocker`, `nda_status`, `nda_blocker`, `trigger_rule_cited`, `policy_citations[]`, `status` all present and correctly typed. `trigger_rule_cited` carries two DPA-TM-001 entries (A-01, E-01) with full hard set (`source_id`, `version`, `row_id`, `trigger_condition`, `citation_class`). `policy_citations` has three entries: two DPA-TM-001 (A-01, E-01) and one ISP-001 §12 NDA clause.
**Status signal:** escalated / escalated — matches scenario-expected (after scenario_2 expected-status correction from `complete` → `escalated` per spec §14 A-07).
**Semantic validity:** PARTIAL PASS — `dpa_required: true` correctly derived from A-01/E-01 matches against `eu_personal_data_flag: true` + `data_subjects_eu: true`; `dpa_blocker: true` consistent with `dpa_required: true` and no executed DPA on record; `nda_status: PENDING` normalized from questionnaire; `nda_blocker: true` consistent with PENDING; `trigger_rule_cited` fully schema-valid with row_id + trigger_condition on both rows; `status: escalated` correct per §14 A-07 (dpa_blocker=true → escalated) and aligned with demo_scenario_02_escalated.md.
**Notes:** Failure is a governance-doc contradiction *inside the spec itself*, not a model or prompt failure. §9 output-contract template (Legal_Agent_Spec.md lines 259-267) shows `policy_citations[]` with a single uniform shape carrying `chunk_id` + `section_id` for both ISP-001 and DPA-TM-001 entries. §11 provenance prose (line 308) says DPA-TM-001 entries carry `row_id` + `trigger_condition`. The two are contradictory, and the model followed the §9 template — emitting DPA-TM-001 entries in `policy_citations` with `chunk_id: "DPA-TM-001__row_A-01"` but `section_id: null` and no `row_id` / `trigger_condition`. Evaluator enforces §11 (per-source-id schema) and hard-failed on four missing keys across the two DPA-TM-001 entries. Soft warning on ISP-001 §12.1.4 absence fired as expected (fixture provides the §12 chunk the model was given, not the §12.1.4 sub-clause — same situation as scenario_1, outside the hard-fail set). Halted per procedure — no re-run. Raw recorded at `tests/recorded_responses/legal_agent__scenario_2__1_fail.json`. Resolution owner: update §9 template to match §11 (either split the template by source_id, or drop the generic `section_id: "string"` placeholder in favor of prose guidance pointing at §11).

## IT Security Agent — scenario_2 — 2026-04-16
**Status:** PASS
**Required fields present:** YES — all STEP-02 required fields present; `policy_citations` entries carry the full hard set (`source_id`, `version`, `section_id`, `citation_class`) plus soft `chunk_id`; `required_security_actions` populated with three structured entries (action_type / reason / owner) consistent with `security_followup_required: true` per §8.4.
**Status signal:** escalated / escalated — matches scenario-expected.
**Semantic validity:** PASS — `data_classification: REGULATED` driven by §8.3 row 1 (`eu_personal_data_present = true`) and supported by the `Employee IDs for EU-based facilities` entry in `regulated_data_types`; `integration_type_normalized: AMBIGUOUS` matches the questionnaire's `erp_type: AMBIGUOUS`; `integration_tier: UNCLASSIFIED_PENDING_REVIEW` correct given unresolved ERP pattern; `fast_track_eligible: false` with `DISALLOWED_AMBIGUOUS_SCOPE` consistent with both REGULATED classification and ambiguous integration (no cross-field violation); citations reference ISP-001 only; `chunk_id` values (`ISP-001__section_12`, `ISP-001__section_17`) match the bundle. Second confirmation of the #40 spec change — model emits `eu_personal_data_present: true` (boolean) on a positive case.
**Notes:** Model picked §8.3 row 1 (EU data → REGULATED) over row 4 (integration ambiguity → AMBIGUOUS) — both rows fire on this bundle, and the first-match ordering produces REGULATED. This is internally consistent: REGULATED captures the data-risk dimension, `integration_type_normalized: AMBIGUOUS` captures the integration-pattern dimension, and `status: escalated` carries the combined uncertainty downstream. Raw recorded at `tests/recorded_responses/it_security_agent__scenario_2__1_pass.json`.

## Legal Agent — scenario_1 — 2026-04-16
**Status:** PASS
**Required fields present:** YES — `dpa_required`, `dpa_blocker`, `nda_status`, `nda_blocker`, `trigger_rule_cited`, `policy_citations[]`, `status` all present and correctly typed; `policy_citations[0]` carries the full hard set (`source_id`, `version`, `section_id`, `citation_class`) plus soft `chunk_id`.
**Status signal:** complete / complete — matches scenario-expected.
**Semantic validity:** PASS — `dpa_required: false` consistent with upstream `data_classification: UNREGULATED` and with no EU data (`eu_personal_data_flag: false`, `data_subjects_eu: false`); `dpa_blocker: false` consistent with `dpa_required: false`; `nda_status: EXECUTED` normalized from questionnaire (not a raw passthrough), `nda_blocker: false` consistent with EXECUTED; `trigger_rule_cited: []` correctly empty given no DPA trigger; `policy_citations` references ISP-001 §12 (NDA clause within Legal-agent retrieval lane); `chunk_id: ISP-001__section_12` matches the bundle's NDA clause chunk verbatim; `citation_class: PRIMARY` valid for a Tier 1 source. First live Legal-agent run after the evaluator realignment in session #43 (`section` → `section_id` + `citation_class`). Fixture hands the model a §12 chunk (not the more specific §12.1.4 sub-clause); model correctly cited what was provided rather than inventing the sub-section identifier — checklist semantic check on §12.1.4 (line 74) is gated on `nda_blocker: true` which does not apply here. Raw recorded at `tests/recorded_responses/legal_agent__scenario_1__1_pass.json`.

## Legal Agent — scenario_2 — 2026-04-16
**Status:** PASS
**Required fields present:** YES — `dpa_required`, `dpa_blocker`, `nda_status`, `nda_blocker`, `trigger_rule_cited`, `policy_citations[]`, `status` all present and correctly typed. `trigger_rule_cited` carries two DPA-TM-001 entries (A-01, E-01) with full §11 key set. `policy_citations` now carries correct **per-source-id shapes**: two DPA-TM-001 entries (A-01, E-01) with `source_id` + `version` + `row_id` + `trigger_condition` + `citation_class`, plus one ISP-001 §12 entry with `source_id` + `version` + `chunk_id` + `section_id` + `citation_class`.
**Status signal:** escalated / escalated — matches scenario-expected (§14 A-07: `dpa_blocker=true` → `status=escalated`).
**Semantic validity:** PASS — `dpa_required: true` correctly derived from A-01 (EU/EEA personal data) + E-01 (logical access to production) matches against `eu_personal_data_flag: true` + `data_subjects_eu: true`; `dpa_blocker: true` consistent with `dpa_required: true` and no executed DPA on record; `nda_status: PENDING` normalized from questionnaire; `nda_blocker: true` consistent with PENDING; `trigger_rule_cited` fully schema-valid on both rows; both `trigger_rule_cited` entries and their `policy_citations` mirrors carry identical `row_id` + `trigger_condition` values — consistent provenance. ISP-001 §12 chunk citation verbatim match with bundle (`chunk_id: "ISP-001__section_12"`, `section_id: "12"`). `status: escalated` correct per §14 A-07 and aligned with demo_scenario_02_escalated.md.
**Notes:** Re-run after the session #46 spec change resolving the §9 vs §11 contradiction (§9 template now shows two per-source-id `policy_citations` entry shapes rather than a single uniform shape). Fixture, evaluator, checklist, and model unchanged vs. run #1 — only the system prompt (spec doc) moved. Result: the spec change took — model now emits DPA-TM-001 entries in `policy_citations` with `row_id` + `trigger_condition` instead of `chunk_id` + `section_id:null`. Soft warn on ISP-001 §12.1.4 absence fired as expected (fixture provides the §12 chunk the model was given, not the §12.1.4 sub-clause — same situation as scenario_1, outside the hard-fail set). Raw recorded at `tests/recorded_responses/legal_agent__scenario_2__2_pass.json`.
**Test depth:** Solid coverage of the escalated happy-path (DPA trigger hit on two rows, NDA PENDING → blocker, §14 A-07 status derivation, dual-source-shape citation discipline), but does **not** exercise the spec's adversarial surfaces — conflicting DPA-TM-001 rows (§12), missing `nda_clause_chunks` (§8.5 escalated branch), upstream `data_classification: AMBIGUOUS` handling, Tier 3 Slack-as-PRIMARY rejection, or any `blocked` path. Meaningful slice of the Legal contract, not a rigorous or exhaustive test.

## Legal Agent — scenario_3 — 2026-04-16
**Status:** PASS
**Required fields present:** YES — `dpa_required`, `dpa_blocker`, `nda_status`, `nda_blocker`, `trigger_rule_cited`, `policy_citations[]`, `status` all present and correctly typed. `trigger_rule_cited` carries one DPA-TM-001 entry (A-01) with the full §11 key set (`source_id`, `version`, `row_id`, `trigger_condition`, `citation_class`). `policy_citations` carries correct per-source-id shapes: one DPA-TM-001 A-01 row-indexed entry and one ISP-001 §12 section-indexed entry.
**Status signal:** complete / complete — matches scenario-expected (§8.5: required evidence present, no escalation/blocked condition applies; §8.3 row 2: `dpa_required = true` AND `existing_dpa_status = EXECUTED` → `dpa_blocker = false`).
**Semantic validity:** PASS — `dpa_required: true` correctly derived from A-01 match against `eu_personal_data_flag: true` + `data_subjects_eu: true`; `dpa_blocker: false` correctly cleared by `existing_dpa_status = "EXECUTED"` per §8.3 row 2 — the required-vs-blocker conflation trap was avoided; `nda_status: EXECUTED` normalized from questionnaire; `nda_blocker: false` consistent with EXECUTED; `trigger_rule_cited` fully schema-valid; DPA-TM-001 `policy_citations` entry mirrors `trigger_rule_cited` exactly on `row_id` + `trigger_condition` — consistent provenance; ISP-001 §12 clause cited correctly for the NDA determination. First live run under the session #50 spec change that named `existing_dpa_status` explicitly; the change landed cleanly — model consumed the questionnaire field and applied §8.3 row 2 without hesitation.
**Notes:** Soft warn on ISP-001 §12.1.4 absence would fire here if checked, but the `nda_blocker: false` branch means the §12.1.4 semantic check (checklist line 74) is not gated in; fixture provides the §12 chunk the model was given, same situation as scenarios 1 and 2 (outside hard-fail set). Raw recorded at `tests/recorded_responses/legal_agent__scenario_3__1_pass.json`.
**Test depth:** Tests a specific and valuable failure mode — the required-vs-blocker conflation (model collapses `dpa_required = true` into `dpa_blocker = true` without reading `existing_dpa_status`). First scenario to exercise §8.3 row 2 (the "already executed" branch) and the `complete` terminal status on a `dpa_required = true` path. Does not exercise what happens when `existing_dpa_status` is absent, PENDING, NOT_STARTED, or UNKNOWN — those variants would need their own fixtures to round out §8.3 coverage.

## Legal Agent — scenario_4 — 2026-04-16
**Status:** PASS
**Required fields present:** N/A — scenario_4 is a `blocked` gate-condition test; determination fields (`dpa_required`, `dpa_blocker`, `nda_status`, `nda_blocker`, `trigger_rule_cited`, `policy_citations`) must be entirely absent from the output per §9.1. All three §9.1 fields present: `status`, `blocked_reason`, `blocked_fields`.
**Status signal:** blocked / blocked — matches scenario-expected.
**§9.1 blocked output shape:** PASS — `blocked_reason: ["MISSING_UPSTREAM_IT_SECURITY_OUTPUT"]` is a non-empty array containing a valid enum value from the §9.1 enum table; correctly identifies the missing upstream STEP-02 output as the gate condition. `blocked_fields: ["data_classification"]` is a non-empty array of canonical field names per CC-001 §15; names the specific upstream field whose absence triggered the block. All six determination fields are entirely absent (not null, not empty — absent), satisfying the §9.1 absent-not-null contract.
**Notes:** This is the second run of scenario_4. Run #1 (session #53) failed because the model emitted all six determination fields alongside `status: blocked` — the spec's §9 language at that time was permissive ("may emit a minimal object"), not prohibitive. Between run #1 and run #2, the Legal Agent Spec was updated from v0.7 → v0.8: §8.5 output shape switching rule added, §9 blocked-output rule rewritten as prohibitive, §9.1 mandatory blocked output shape added with enum table and blocked_fields semantics, §12 blocked rows updated to reference §9.1, §13 Example E added, §14 A-09 acceptance check added. The evaluator (`per_agent_test_env/evaluators.py`) and smoke tests (`tests/per_agent_live/test_runner_smoke.py`) were also updated to enforce the §9.1 contract. Fixture, model, and bundle unchanged between runs — only the system prompt (spec doc) and evaluation logic moved. Result: the spec change took — model now emits the §9.1 blocked shape cleanly with no determination field leakage. Raw recorded at `tests/recorded_responses/legal_agent__scenario_4__2_pass.json`.
**Test depth:** Validates the core blocked gate condition (missing upstream STEP-02 output → bundle inadmissible → blocked) and the §9.1 output shape switching discipline. Does not exercise multiple simultaneous blocked reasons (e.g., `MISSING_UPSTREAM_IT_SECURITY_OUTPUT` + `MISSING_QUESTIONNAIRE_EU_FIELDS`), does not test the boundary where some but not all upstream fields are present, and does not test blocked → escalated precedence if both conditions could apply. The scenario is narrow by design — it isolates the single most important blocked path (no STEP-02 output at all) to confirm the agent respects the §8.5 output shape switching rule.

## Legal Agent — scenario_5 — 2026-04-16
**Status:** FAIL
**Required fields present:** YES — all six determination fields present (`dpa_required`, `dpa_blocker`, `nda_status`, `nda_blocker`, `trigger_rule_cited`, `policy_citations`) plus `status`. Correct escalated shape (no blocked-shape leakage).
**Status signal:** escalated / escalated — matches scenario-expected.
**Semantic validity:** FAIL — model silently resolved the Tier 1 DPA conflict instead of escalating it. Two DPA-TM-001 rows in the bundle: A-01 (REQUIRED — EU personal data processing) and A-06 (NOT REQUIRED — anonymized operational scheduling metadata). Both apply to the vendor profile but reach contradictory trigger outcomes. Per CC-001 §4.1, a Tier 1 vs Tier 1 conflict cannot be auto-suppressed; per §8.5 and §9.2, the DPA determination fields must be null. Model emitted `dpa_required: true`, `dpa_blocker: true`, `trigger_rule_cited: [A-01]` — it picked A-01 and dropped A-06 without acknowledging the conflict. The model escalated via §8.5 condition 4 (`dpa_blocker=true`) rather than condition 2 (Tier 1 conflict).
**Notes:** NDA determination correctly resolved: `nda_status: EXECUTED`, `nda_blocker: false`, ISP-001 §12.1.4 cited in `policy_citations`. The model's NDA path is clean — failure is isolated to the DPA conflict recognition. Soft warning fired: `policy_citations` missing A-06 conflicting row (spec tension between §9 note and §9 output field constraints — logged as warning, not failure). The model appears to have treated A-06's anonymization-exclusion framing as inapplicable rather than conflicting, suggesting it reasoned through the rows substantively instead of applying the mechanical Tier 1 parity rule from CC-001 §4.1. Raw recorded at `tests/recorded_responses/legal_agent__scenario_5__1_fail.json`. Halted per procedure — no re-run.
**Test depth:** This is the first scenario to exercise the Tier 1 conflict escalation pathway (§8.5 condition 2) and the §9.2 partial-null determination shape (DPA null, NDA resolved). The failure confirms the model does not yet reliably apply the authority-tier conflict non-suppressibility rule. The A-06 row was intentionally designed with a plausible substantive distinction (anonymized data → GDPR Recital 26 exclusion) to test whether the model follows the mechanical parity rule or reasons through the substance — it reasoned through the substance and picked a winner, which is exactly the behavior the spec prohibits. Future remediation options: (1) strengthen the spec's conflict-detection guidance in the system prompt, (2) add explicit conflict-detection examples, or (3) restructure the bundle to make the conflict more syntactically obvious.

## Legal Agent — scenario_5 — 2026-04-18
**Status:** PASS
**Required fields present:** YES — all six determination fields present (`dpa_required`, `dpa_blocker`, `nda_status`, `nda_blocker`, `trigger_rule_cited`, `policy_citations`) plus `status`. Correct §9.2 escalated shape (no blocked-shape leakage: `blocked_reason` and `blocked_fields` absent).
**Status signal:** escalated / escalated — matches scenario-expected.
**Semantic validity:** PASS — model correctly recognized the Tier 1 DPA conflict and emitted the partial-null §9.2 shape. `dpa_required: null`, `dpa_blocker: null`, `trigger_rule_cited: null` — no silent row-picking, no citation for an unresolved determination. `nda_status: "EXECUTED"` normalized from questionnaire, `nda_blocker: false` consistent with EXECUTED, `policy_citations` contains the ISP-001 §12.1.4 NDA citation (resolved determination) and omits DPA-TM-001 citations (unresolvable determination). NDA path fully clean, isolated from the DPA conflict.
**Notes:** Re-run after two upstream changes. (1) Spec tightening: Legal_Agent_Spec.md bumped v0.9 → v0.10 — §8.5 gained a clarifying note defining "conflict on the same trigger question" as two or more Tier 1 rows whose trigger conditions both evaluate to true on the same confirmed fact pattern and disagree on outcome (complementary rules covering disjoint fact patterns are explicitly not conflicts). (2) Fixture rebuild: the old A-06 row (narrow anonymization carve-out) was replaced with a new A-07 row designed so both A-01 and A-07 fire on OptiChain's exact evidence (EU/EEA personal data for workforce-adjacent analytics) with no factual disambiguation — the conflict is in the legal conclusion (A-01 REQUIRED under Art. 28 vs. A-07 NOT REQUIRED via Art. 6(1)(f) legitimate interest / controller-to-controller allocation), not the factual predicate. Source rebuilt to `scenario_data/scenario_5/sources/DPA-TM-001_scenario5.json` (28 rows: full production v2.1 set re-versioned as `2.1-scenario5` + new A-07). Scenario-scoped Chroma collection `idx_dpa_matrix__scenario5` and BM25 bundle built under `scenario_data/scenario_5/`; production indices untouched. Retrieval-only pre-check confirmed both A-01 and A-07 surface in the candidate set. Fixture `tests/fixtures/bundles/step_03_scenario_5.json` regenerated with `dpa_trigger_rows = [A-01, A-07]`. Evaluator A-06 references updated to A-07. Soft warning fired as expected (§9 note vs. §9 output-field constraint tension on whether to cite both conflicting rows when the determination is unresolvable) — model followed the "omit citations for unresolved determinations" interpretation, logged as warning not failure. Raw recorded at `tests/recorded_responses/legal_agent__scenario_5__2_pass.json`.
**Test depth:** First PASS on the Tier 1 conflict escalation pathway (§8.5 condition 2) and the §9.2 partial-null determination shape (DPA null, NDA resolved). Validates that with a correctly-designed conflict fixture (both rows fire on the same evidence; disagreement is in the legal conclusion) and a correctly-scoped spec (§8.5 conflict definition), the model applies the authority-tier conflict non-suppressibility rule from CC-001 §4.1 rather than reasoning through to a winner. Does not yet exercise: multiple simultaneous conflict pairs, three-way Tier 1 conflicts, or conflicts where one row is Tier 1 and one is lower-tier (which per CC-001 §4.1 *is* auto-suppressible — the opposite of this test). Meaningful slice of the conflict-handling contract, not exhaustive coverage.

## Legal Agent — scenario_6 — 2026-04-18
**Status:** FAIL
**Required fields present:** YES — all six determination fields present (`dpa_required`, `dpa_blocker`, `nda_status`, `nda_blocker`, `trigger_rule_cited`, `policy_citations`) plus `status`. Correct §9.2 escalated shape (no blocked-shape leakage: `blocked_reason` and `blocked_fields` absent).
**Status signal:** escalated / escalated — matches scenario-expected.
**Semantic validity:** PARTIAL PASS — DPA determination fully correct: `dpa_required: true` derived from A-01 (EU/EEA personal data) + E-01 (logical access) against `eu_personal_data_flag: true` + `data_subjects_eu: true`; `dpa_blocker: false` correctly cleared by `existing_dpa_status: "EXECUTED"` per §8.3 row 2; `trigger_rule_cited` carries both A-01 and E-01 with PRIMARY + `row_id` + `trigger_condition`; `policy_citations` carries both DPA-TM-001 entries (hallucination guard passed — no ISP-001 §12.1.4 fabrication despite that being the obvious clause to invent). NDA determination failed the §9.2 per-field null contract: model emitted `nda_status: "UNKNOWN"` and `nda_blocker: true` instead of the required `nda_status: null` / `nda_blocker: null` for the dual-absence case.
**Notes:** Failure is a governance-doc contradiction *inside the spec itself*, not a model or prompt failure. §8.4 (general NDA normalization) says absent questionnaire → `nda_status: "UNKNOWN"` with derived `nda_blocker: true`. §9.2 (escalated-output per-field null rules) says both `existing_nda_status` AND `nda_clause_chunks` absent → `nda_status: null`, `nda_blocker: null` (no evidence to assess). The spec does not explicitly order the two — §9.2 is the specific escalated-output rule, §8.4 is the general normalization. Evaluator comment at `per_agent_test_env/evaluators.py:700–705` calls out this exact tension and asserts §9.2 precedence, but the asserted precedence is not reflected in the spec text the model reads. Model applied §8.4 — a defensible read of the current spec. Soft warning fired as expected (`nda_blocker=true` with no ISP-001 §12.1.4 citation in `policy_citations` — correct refusal to hallucinate, but consistent with the overall NDA resolution the model should not have produced). Raw recorded at `tests/recorded_responses/legal_agent__scenario_6__1_fail.json`. Halted per procedure — no re-run. Resolution owner: add explicit precedence language tying §9.2 per-field null rules above §8.4 general normalization on escalated runs when evidence is dually absent, or add an A-* acceptance-check row + §9.2 worked example covering the dual-absence NDA case.
**Test depth:** First scenario to exercise (a) the §8.5 condition 5 escalation path (`nda_clause_chunks` absent from bundle), (b) the §9.2 partial-null shape with DPA resolved and NDA null (mirror of scenario_5's DPA-null/NDA-resolved partition), and (c) the clause-hallucination guard for NDA evidence. Hallucination guard was the highest-value latent risk (model had strong priors to cite §12.1.4 as it did in scenarios 1–3) and the model passed it cleanly. The §8.4 vs §9.2 ambiguity is a real-world spec-drift surface that this scenario flushed out; fixing the spec is the correct path.

## Legal Agent — scenario_6 — 2026-04-18
**Status:** PASS
**Required fields present:** YES — all six determination fields present (`dpa_required`, `dpa_blocker`, `nda_status`, `nda_blocker`, `trigger_rule_cited`, `policy_citations`) plus `status`. §9.2 escalated shape clean (no `blocked_reason` / `blocked_fields` leakage).
**Status signal:** escalated / escalated — matches scenario-expected.
**Semantic validity:** PASS — DPA determination: `dpa_required: true` from A-01 (EU/EEA personal data) against `eu_personal_data_flag: true` + `data_subjects_eu: true`; `dpa_blocker: false` correctly cleared by `existing_dpa_status: "EXECUTED"` per §8.3 row 2. NDA determination: `nda_status: null`, `nda_blocker: null` per §9.2 dual-absence rule (both `existing_nda_status` absent from questionnaire AND `nda_clause_chunks` absent from bundle). `trigger_rule_cited` carries A-01 with `row_id` + `trigger_condition` + PRIMARY; `policy_citations` contains only the A-01 PRIMARY DPA citation — hallucination guard held (no ISP-001 §12.1.4 fabrication).
**Notes:** Re-run after spec v0.10 → v0.11 update in session immediately prior. Spec-only change: added explicit §8.4 / §9.2 precedence language (§9.2 per-field null rules override §8.4 general normalization on escalated runs when NDA evidence is dually absent) plus a new §9.2 worked example ("DPA resolved, NDA dually absent") showing exactly this JSON shape. Fixture, model (claude-haiku-4-5), and evaluator unchanged between run #1 and run #2. Minor drift vs. run #1: model cited only A-01 in `trigger_rule_cited` / `policy_citations` where run #1 cited A-01 + E-01 — both are defensible reads (A-01 alone is sufficient to establish `dpa_required: true`; E-01 is a separate logical-access trigger that also fires). Not a hard-check concern. Raw recorded at `tests/recorded_responses/legal_agent__scenario_6__2_pass.json`.
**Test depth:** Confirms that the §8.4 / §9.2 ambiguity flushed out by run #1 is fixed at the spec level rather than patched at the evaluator or prompt level — the model now reads an unambiguous contract and produces the correct null-pair. Closes the NDA-resolution half of the §9.2 partial-null contract (scenario_5 covered DPA-null/NDA-resolved; scenario_6 now covers DPA-resolved/NDA-null). Hallucination guard on ISP-001 §12.1.4 held for the second time on this scenario — strong signal that the model does not fabricate NDA clause citations when evidence is absent, independent of the §8.4 vs §9.2 reading.

## Procurement Agent — scenario_7 — 2026-04-18
**Status:** FAIL
**Required fields present:** YES — all five determination fields plus `status` present. Shape is standard §9 (not §9.1 blocked shape). `blocked_reason` / `blocked_fields` absent (no shape leakage). `required_approvals[]` entries carry `approver` + `domain` + `status` + `blocker` + `estimated_completion`. `policy_citations` entries carry the full hard set (`source_id`, `version`, `chunk_id`, `row_id`, `approval_path_condition`, `citation_class`).
**Status signal:** expected `escalated` / got `complete` — **scenario-expected status miss.**
**Semantic validity:** FAIL (silent path fabrication — the single highest-risk procurement failure mode). The scenario-scoped PAM-001 matrix contains only rows A-T1, B-T1, C-T1 (Classes A/B/C at Tier 1). The questionnaire `vendor_class: "Class D — Technology Professional Services"` has zero Class coverage in the subset. Per §8.5 ("No PAM-001 row matches the vendor/deal combination" → escalated) and §9.2 Example C, the expected output is `status: escalated` with `approval_path: null`, `required_approvals: null`, `estimated_timeline: null`, `policy_citations: null`, `fast_track_eligible: true` (passthrough). Instead, the model emitted `approval_path: "FAST_TRACK"` with a PRIMARY PAM-001 citation of row C-T1 and asserted `status: complete`. The model's own `approval_path_condition` string ("UNREGULATED vendor, FAST_TRACK eligible, no DPA required, standard NDA applies") reveals the reasoning path: it pattern-matched on `data_classification`, `fast_track_eligible`, `dpa_required`, and `nda_status` and found C-T1 as the best non-regulated fast-track fit — **ignoring the Class mismatch entirely**. `fast_track_eligible: true` was passed through correctly (A-02 held), but that guarantee is hollow when `approval_path` is fabricated alongside it.
**Notes:** Failure is a model reasoning / spec-gap failure, not a harness or evaluator issue. Spec §3 lists `vendor_class` as a required admissibility field; §8.3 says "approval path determined by applying PAM-001 rows to the authoritative vendor profile"; §14 A-04 says "No `approval_path` asserted when no PAM-001 row matches" with pass condition "agent emits `escalated` rather than inferring a path from non-matrix evidence." But the spec does not explicitly state that `vendor_class` is a **strict** matrix lookup key — i.e., that a row with `Class: C` does not apply to a `vendor_class: Class D` profile under any circumstance. The PAM-001 row text format ("Class: A\nTier: T1\n...") makes the Class explicit in each row but does not prohibit cross-Class application. The model read Class as soft vendor-context rather than as a hard primary key, and chose the nearest fast-track-eligible row. Analogous to the scenario_5 Legal failure where the model reasoned substantively past a narrow row — same failure pattern. Halted per procedure — no re-run. Raw recorded at `tests/recorded_responses/procurement_agent__scenario_7__1_fail.json`. Resolution owner: spec update — §8.3 and/or §14 A-04 should state explicitly that `vendor_class` and `integration_tier` are strict PAM-001 row primary keys, and that cross-Class or cross-Tier matching is not permissible (mirror the scenario_5 / v0.10 §8.5 Tier 1 conflict clarification pattern). Optionally, §9.2 could add a worked example of the "no Class match" escalation to reinforce §14 A-04 Example C.
**Test depth:** First scenario for the Procurement Agent to exercise the §8.5 no-matrix-match escalation path and the §9.2 Example C shape (all determination fields null except fast_track_eligible passthrough). Also first procurement scenario to exercise the citation-hallucination guard on PAM-001 row selection. Does not yet exercise: the two-row PAM-001 conflict path (§8.5 condition 5, analogous to scenario_5), the blocked-shape path (§9.1 with MISSING_QUESTIONNAIRE_VENDOR_FIELDS or MISSING_PAM_001), or the inherited-upstream-escalation path (§8.5 condition 6, where STEP-03 is escalated but Procurement can still resolve). Validates that with a correctly-designed no-match fixture and clean upstream inputs, the failure is at the spec clarity level rather than the harness level — the evaluator is strict and caught the fabrication cleanly.

## Procurement Agent — scenario_7 — 2026-04-18
**Status:** PASS
**Required fields present:** YES — all five determination fields (`approval_path`, `required_approvals`, `estimated_timeline`, `policy_citations`, `fast_track_eligible`) plus `status` present. §9.2 escalated shape clean — no `blocked_reason` / `blocked_fields` leakage.
**Status signal:** escalated / escalated — matches scenario-expected.
**Semantic validity:** PASS — `status: escalated` correctly derived per the new §8.5 row ("No PAM-001 row matches on **both** `vendor_class` and `integration_tier` primary keys → escalated"). `approval_path: null`, `required_approvals: null`, `estimated_timeline: null`, `policy_citations: null` per §9.2 — no nearest-neighbor substitution, no fabricated path, no PAM-001 PRIMARY citation invented. `fast_track_eligible: true` correctly passed through from upstream IT Security output (A-02 passthrough preserved — not a hollow guarantee this time; the field is true *and* the downstream determination correctly escalates rather than inferring a fast-track path on top of it).
**Notes:** Re-run after spec v0.7 → v0.8 update in session immediately prior. Spec-only change: (1) §8.3 — added "Strict primary-key matching" paragraph stating PAM-001 row matching is strict lookup on `vendor_class` and `integration_tier` as primary keys; partial matches on secondary dimensions (`data_classification`, `fast_track_eligible`, `dpa_required`, `nda_status`) do not constitute a row match; no-match condition escalates per §8.5 with `approval_path: null` per §9.2, not nearest-in-semantic-similarity substitution. (2) §8.5 — added explicit status-derivation row for the primary-key-mismatch case. (3) §14 A-04 — expanded pass condition to explicitly call out partial-dimension match as an A-04 violation. Fixture, model (claude-haiku-4-5), evaluator, and bundle unchanged between run #1 and run #2 — only the system prompt (spec doc) moved. Result: the spec change took — model now escalates cleanly rather than pattern-matching on secondary dimensions to select C-T1. Raw recorded at `tests/recorded_responses/procurement_agent__scenario_7__2_pass.json`.
**Test depth:** Confirms that the §8.3 primary-key ambiguity flushed out by run #1 is fixed at the spec level rather than patched at the evaluator or prompt level — the model now reads an unambiguous contract ("vendor_class and integration_tier are strict primary keys") and produces the correct §9.2 no-match escalation. Closes the silent-path-fabrication failure mode at the spec level for the no-row-match case. Still does not exercise: the two-row PAM-001 conflict path (§8.5 condition 5, analogous to Legal scenario_5), the blocked-shape path (§9.1 with MISSING_QUESTIONNAIRE_VENDOR_FIELDS or MISSING_PAM_001), or the inherited-upstream-escalation path (§8.5 condition 6, where STEP-03 is escalated but Procurement can still resolve). First PASS of the spec-clarification-after-FAIL cycle for the Procurement Agent, mirroring the Legal Agent scenario_5 and scenario_6 clarification cycles — same pattern, same resolution shape (fix the spec, not the prompt or evaluator).

## Procurement Agent — scenario_1 — 2026-04-19
**Status:** FAIL (semantic — runner initially reported PASS before evaluator patch)
**Required fields present:** YES — all five determination fields (`approval_path`, `required_approvals`, `estimated_timeline`, `policy_citations`, `fast_track_eligible`) plus `status` present. Shape is the standard §9 COMPLETE shape. `blocked_reason` / `blocked_fields` absent. `required_approvals[]` entries carry `approver` + `domain` + `status` + `blocker` + `estimated_completion`. `policy_citations[0]` carries the full hard set (`source_id`, `version`, `chunk_id`, `row_id`, `approval_path_condition`, `citation_class`).
**Status signal:** complete / complete — matches scenario-expected.
**Semantic validity:** FAIL (silent wrong-row selection — the `complete`-path mirror of scenario_7's silent path fabrication failure). The questionnaire carries `vendor_class: "Class A — Enterprise Platform"` and the upstream IT Security output carries `integration_tier: "TIER_3"`. Per §8.3 strict primary-key matching (added in v0.8), the only valid PAM-001 row is **A-T3** (Class A × Tier T3). The Supervisor-assembled candidate set includes A-T3 explicitly alongside five adversarial neighbors: A-T2, A-T1, B-T3, B-T2, E-T2. The model selected **A-T1** as its PRIMARY PAM-001 citation — Tier T1, not Tier T3 — and assembled the A-T1 approvers (CISO + Procurement Director + Director+ business owner) instead of A-T3's required heavyweight set (CISO co-sign + GC co-sign + CPO sign-off + VP + SVP). The emitted `approval_path_condition` string on the citation even self-labels as `"Class A, Tier T1, UNREGULATED, NDA executed, no DPA required, no blockers"` — the model knew it picked T1 and did so anyway. `approval_path: "STANDARD"` is enum-valid but asserted from a row that does not match the vendor profile's primary keys. `fast_track_eligible: true` passthrough held correctly (A-02 preserved) — but the correctness of that passthrough is hollow when the entire approval path determination is derived from a wrong-row substitution.
**Notes:** Failure is a model reasoning failure against the v0.8 spec, plus an evaluator coverage gap that initially surfaced as PASS. The v0.8 §8.3 clarification ("a row with `Tier: T1` does not apply to an `integration_tier: "TIER_3"` profile") is explicit and should have been sufficient, yet the model still substituted A-T1 for A-T3 when the matching row was present in the candidate set alongside lighter alternatives. Scenario_7's v0.8 fix closed the "no matching row at all" surface but not this adjacent surface where a matching row exists AND lighter-weight same-Class alternatives are present. Evaluator gap: the generic procurement checks validated shape (enum / non-empty arrays / PRIMARY PAM-001 presence / row_id non-empty) but never cross-checked the cited `row_id` against the questionnaire + IT Security primary keys. Scenario_1-specific hard check added at `per_agent_test_env/evaluators.py` after the scenario_7 block: if status=complete on scenario_1, the cited PRIMARY PAM-001 row_id must equal `"A-T3"`; multiple PRIMARY PAM-001 citations also hard-fail (a COMPLETE determination selects one governing row); soft-warn on rows outside the candidate set {A-T3, A-T2, A-T1, B-T3, B-T2, E-T2} for hallucination. Replayed the recorded response through the patched evaluator — hard-fails with the row-identity message. Recorded file renamed from `procurement_agent__scenario_1__1_pass.json` to `procurement_agent__scenario_1__1_fail.json` to match scenario_7 convention. Halted per procedure — no re-run. Resolution owner: spec update (not yet applied per user directive) — §8.3 or §14 A-04 should additionally state: "if the Supervisor-assembled candidate set contains at least one row matching on both primary keys, the agent must select that row; substituting a non-matching row when a matching row is present is an A-04 violation." This extends the current v0.8 §8.3 rule (which forbids substitution when no match exists) to the complementary case (match exists in the candidate set, model still substitutes).
**Test depth:** First `complete`-path scenario for the Procurement Agent with a populated Supervisor-assembled candidate set (prior to this session, both scenario_1 and scenario_2 fixtures had empty `approval_path_matrix_rows`). Populated via hybrid retrieval (MiniLM vector + BM25) against the scenario_1 production `idx_procurement_matrix` with RRF k=60 fusion — A-T3 landed at rank 5 by RRF, not rank 1, which is the adversarial property the test relies on (a top-rank-selection heuristic would pick A-T2, not A-T3). Also populated `slack_procurement_chunks` with SLK-001 thread T3 (OptiChain review kickoff notes) as SUPPORTING context to exercise the Tier 3 supplementary citation surface. Exercised for the first time: the PAM-001 row-identity cross-check against questionnaire + IT Security primary keys (new scenario_1 hard check), the heavyweight STANDARD — ENHANCED approval path (A-T3: CISO + GC + CPO + VP + SVP co-sign), and the fast_track/NOT-ELIGIBLE divergence surface (upstream fast_track_eligible=true but PAM-001 row A-T3 says NOT ELIGIBLE; per A-02 the field passes through unchanged and the `approval_path` reflects the row, not the upstream flag). Does not yet exercise: the A-02 passthrough failure mode where the model correctly picks the row but demotes fast_track_eligible (we got the inverse failure — wrong row, correct passthrough). Future work: the spec extension for "matching row exists in candidate set" is a clear next step; a follow-up scenario could stress-test the specific adversarial ordering of candidates more systematically (e.g., matching row at rank 6, lighter Class A alternatives at ranks 1–5).

## Procurement Agent — scenario_1 — 2026-04-19
**Status:** FAIL (second run — model picked a different wrong row, confirming the spec gap is systematic rather than stochastic)
**Required fields present:** YES — all five determination fields (`approval_path`, `required_approvals`, `estimated_timeline`, `policy_citations`, `fast_track_eligible`) plus `status` present. Standard §9 COMPLETE shape. `blocked_reason` / `blocked_fields` absent. `required_approvals[]` carries `approver` + `domain` + `status` + `blocker` + `estimated_completion`. `policy_citations[0]` carries the full hard set.
**Status signal:** complete / complete — matches scenario-expected.
**Semantic validity:** FAIL — silent wrong-row selection, **different and worse failure than run #1**. Run #1: cited A-T1 (Class match, Tier mismatch — substitution within Class). Run #2: cited **E-T2** (Class AND Tier mismatch — substitution across both primary keys). The model's self-authored `approval_path_condition` on the citation reads `"Class E, Tier T2, UNREGULATED data, fast-track eligible, no system access, no regulated data in scope"` — it knew it was Class E and picked it anyway. Approval path demoted further from STANDARD (run #1) to `FAST_TRACK` (run #2), with only a single `Procurement Manager` approver. The pattern across the two runs reveals the model's underlying heuristic: when upstream `fast_track_eligible: true` is present, the model locates the candidate row most consistent with a fast-track posture (E-T2 is likely the only FAST_TRACK-eligible row in the candidate set) and substitutes it for the primary-key match, regardless of Class × Tier mismatch. `fast_track_eligible: true` passthrough held in both runs (A-02 preserved) — but this is hollow consistency when the entire row-selection step is bypassing primary keys to align with the passthrough.
**Notes:** No code, prompt, fixture, or spec changes between run #1 and run #2 — only the evaluator was patched to hard-fail the row-identity mismatch and the prior recorded file was renamed `__1_pass.json` → `__1_fail.json`. The evaluator patch worked exactly as intended (row-identity hard check caught the violation immediately). The model's divergence in which wrong row it picks across runs (A-T1 vs. E-T2) is stochastic; the underlying failure mode (substituting a non-primary-key-matching row when a matching row is present in the candidate set) is systematic. The v0.8 §8.3 strict primary-key matching clarification, which was sufficient to close the scenario_7 "no matching row exists" case, is **not sufficient** to close this adjacent case where a matching row exists AND the upstream `fast_track_eligible` signal tempts the model toward a FAST_TRACK-eligible alternative. Spec unchanged per user directive. Raw recorded at `tests/recorded_responses/procurement_agent__scenario_1__2_fail.json`. Halted per procedure — no further re-run.
**Test depth:** Second data point on the silent wrong-row substitution failure mode. Run #1 showed same-Class-different-Tier substitution (mild); run #2 showed different-Class-different-Tier substitution (severe). The transition from A-T1 to E-T2 across runs strongly suggests the model is reasoning from the upstream `fast_track_eligible: true` signal and selecting the row most consistent with that signal, rather than applying the strict primary-key match rule from §8.3 as the first and binding step. Closing this requires either: (a) a §8.3 extension making primary-key matching the *first* step that cannot be bypassed by any upstream signal ("if the Supervisor-assembled candidate set contains at least one row matching on both primary keys, the agent MUST select that row before applying any other matrix routing logic — upstream `fast_track_eligible` influences which approval_path enum is emitted after the row is selected, not which row is selected"), and/or (b) a §8.3 or §14 A-04 example showing the specific substitution pattern (matching row present, model picks FAST_TRACK-eligible non-matching row to align with upstream flag) as a concrete A-04 violation. The v0.8 language addresses the "no match exists" case but leaves the "match exists but tempting alternative present" case to be inferred by the model, and the model does not reliably infer it.

## Procurement Agent — scenario_1 — 2026-04-19 (post enum-fix re-run)
**Status:** FAIL (third run — post PAM-001 re-index from v3.0 CSV)
**Required fields present:** YES — all five determination fields plus `status` present. Standard §9 COMPLETE shape. `blocked_reason` / `blocked_fields` absent. Hard citation field set complete.
**Status signal:** complete / complete — matches scenario-expected.
**Semantic validity:** FAIL — **enum-conflict hypothesis refuted**. Prior hypothesis: the model avoided A-T3 because the indexed PAM-001 rendered A-T3's `approval_path` as the non-enum value "STANDARD — ENHANCED" (and likewise "EXECUTIVE APPROVAL" on every T4 row), so citing A-T3 would have forced a schema-violating output. Re-indexed `data/processed/scenario_1/chunks/PAM-001.json` against the authoritative `scenario_1_mock_documents/Procurement_Approval_Matrix_v3_0.csv`, which contains only the two spec-conformant enum values (`STANDARD`, `FAST_TRACK`) — 8 rows corrected: A-T3, A-T4, B-T3, B-T4, C-T4, D-T3, D-T4, E-T4 (all collapsed to `STANDARD`, with tier-level severity preserved in the Review / Sign-off / Notes columns). Scenario_1 fixture bundle also corrected for A-T3 and B-T3 text (the only two affected rows present in the candidate set). Model still cited **A-T1** with `approval_path: "STANDARD"` — the exact same wrong row as run #1, with the exact same shape. `approval_path_condition` self-label: `"Class A vendor, Tier T1 integration, STANDARD approval path"` — the model is now explicitly claiming the integration is Tier T1, directly contradicting IT Security's `integration_tier: "TIER_3"`. `fast_track_eligible: true` passthrough still held (A-02 preserved — hollow again).
**Notes:** The enum fix was independently correct — the indexed PAM-001 had been built from a pre-refactor source and carried approval_path values that do not appear in the spec v0.6 enum. That data-quality bug is now resolved regardless of the scenario outcome. But it was **not** the root cause of the wrong-row selection: with A-T3 now rendered as clean `STANDARD` (identical enum value to A-T1, lighter tier), the model still picks A-T1. This refutes the "model avoiding A-T3 because of a schema conflict" hypothesis and reinforces the run #1 / run #2 conclusion that the model reasons from upstream signals (fast_track_eligible, data_classification, dpa status) toward a lightest-fit row rather than applying primary-key matching as a strict first step. The spec-extension resolution already noted after run #2 remains the right next move. Separately, finding #4 from the external review is still open: the scenario fixture has `fast_track_eligible: true` from IT Security but every Class A row in PAM-001 says `NOT ELIGIBLE` — a genuine upstream contradiction in the fixture itself. Raw recorded at `tests/recorded_responses/procurement_agent__scenario_1__3_fail_post_enum_fix.json`.
**Test depth:** Third data point on the silent wrong-row substitution failure mode, this run specifically controlled for the enum-conflict hypothesis. With that variable removed, the substitution pattern persists unchanged, isolating the failure to the model's row-selection reasoning rather than the downstream enum-serialization stage. Also establishes that re-indexing PAM-001 from the canonical v3.0 CSV is a safe, reversible data-quality fix independent of the scenario_1 spec-gap resolution. Remaining open items: (a) the §8.3 / §14 A-04 extension covering "matching row present in candidate set alongside tempting fast-track alternative" still pending; (b) the IT Security `fast_track_eligible: true` + Class A "NOT ELIGIBLE" fixture contradiction still pending (finding #4); (c) other scenarios that use scenario_1's PAM-001 chunks should be re-verified against the corrected file.

## Procurement Agent — scenario_1 — 2026-04-19 (post re-baseline to Class C × T1)
**Status:** PASS
**Required fields present:** YES — all five determination fields (`approval_path`, `required_approvals`, `estimated_timeline`, `policy_citations`, `fast_track_eligible`) plus `status` present. Standard §9 COMPLETE shape. `blocked_reason` / `blocked_fields` absent. `required_approvals[]` entries carry `approver` + `domain` + `status` + `blocker` + `estimated_completion`. `policy_citations[0]` carries the full hard set (`source_id`, `version`, `chunk_id`, `row_id`, `approval_path_condition`, `citation_class`).
**Status signal:** complete / complete — matches scenario-expected.
**Semantic validity:** PASS — model cited **C-T1** as PRIMARY PAM-001 with `approval_path: "FAST_TRACK"`, two Manager-level approvers (Procurement Manager + Business Owner Manager), and `approval_path_condition: "Class C, Tier T1, UNREGULATED, FAST_TRACK eligible, no DPA required, standard NDA applies"`. All downstream determination fields consistent with C-T1 row content. `fast_track_eligible: true` passthrough aligns with the row's own `Fast-Track Eligible?: ELIGIBLE` flag — A-02 passthrough is substantive rather than hollow this time (upstream flag + row flag + downstream approval path are mutually consistent).
**Notes:** Scenario re-baselined after runs #1–3 revealed that the original fixture design carried an internal contradiction (`vendor_class: "Class A"` + IT Security `fast_track_eligible: true` with every Class A row in PAM-001 saying `NOT ELIGIBLE`) that the model resolved by silently substituting lighter-weight rows to align with the upstream fast-track signal. Root-cause diagnosis pointed at two separable issues: (a) the indexed PAM-001.json had been built from a pre-v3.0 source and carried enum-violating `approval_path` strings ("STANDARD — ENHANCED", "EXECUTIVE APPROVAL") — this was a data-quality bug and is addressed separately below; (b) the fixture profile + upstream signals were logically inconsistent — no T3 row in any Class is fast-track ELIGIBLE, so the adversarial "Class A × T3 + fast_track_eligible=true" setup forced the model into an unwinnable position. Chose the "re-baseline to a clean fast-track path" resolution per user direction: moved the scenario to Class C × T1 (a row that IS fast-track ELIGIBLE), aligning the questionnaire, upstream IT Security output, and PAM row content. Result: clean PASS on first attempt with no spec change required. Raw recorded at `tests/recorded_responses/procurement_agent__scenario_1__4_pass.json`.
**Test depth:** First PASS for the Procurement Agent on scenario_1. Exercises the clean fast-track path end-to-end: `vendor_class: "Class C — Non-regulated Software"` × `integration_tier: "TIER_1"` → C-T1 row → `approval_path: "FAST_TRACK"` → Manager-level approvers → 3–5 business day timeline. Also exercises: the §8.3 strict primary-key matching rule under a non-adversarial profile (C-T1 is the unambiguous RRF rank-1 candidate), A-02 fast_track_eligible passthrough under substantive consistency (no divergence between upstream flag and row flag), and the populated-candidate-set retrieval path (RRF top-6 assembled from fresh retrieval against the re-indexed PAM-001 v2.0). Candidate set: C-T1 (rank 1, primary-key match), E-T1 (rank 2), C-T2 (rank 3), E-T2 (rank 4), A-T1 (rank 5), A-T4 (rank 6). Does not yet exercise: the two-row PAM-001 conflict path (§8.5 condition 5), the blocked-shape path (§9.1), the inherited-upstream-escalation path (§8.5 condition 6), nor the adversarial "matching row + tempting lighter-weight alternative" surface that runs #1–3 explored (that surface is now relegated to a future dedicated scenario where the contradiction is intentional and paired with explicit spec language).

**Side effect — data-quality fix:** `data/processed/scenario_1/chunks/PAM-001.json` was rebuilt from `scenario_1_mock_documents/Procurement_Approval_Matrix_v2_0.csv` (v2.0, 20 rows). Vector index `idx_procurement_matrix` (Chroma + BM25) rebuilt in `data/indexes/scenario_1/` and `data/bm25/scenario_1/`. Index registry PAM-001.version updated 3.0 → 2.0 to match the source of truth. The v2 CSV contains `approval_path` values "STANDARD — ENHANCED" (on A-T3, B-T3, D-T3) and "EXECUTIVE APPROVAL" (on all T4 rows). These are NOT in the spec v0.6 enum (`STANDARD | FAST_TRACK`). The scenario_1 PASS does not exercise these non-enum rows (C-T1 is clean `FAST_TRACK`), but any future Procurement scenario that routes through a T3 (non-E) row or any T4 row against this indexed data will hit the same enum-conflict surface the external review flagged earlier. Open item: decide whether to extend the spec enum to match v2 semantics (keep "STANDARD — ENHANCED" / "EXECUTIVE APPROVAL" as valid enum values, reflecting tier-level severity in the enum itself) or re-index from a spec-conformant CSV (fold tier severity into the Review / Sign-off / Notes columns only, keep enum binary). Rebuild script committed at `scripts/rebuild_scenario_1_pam.py` for repeatability.
